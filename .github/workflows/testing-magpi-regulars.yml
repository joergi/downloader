# this will test the downloader script for MagPI which is called by:
# The MagPi
#   The MagPi regular issues (Mac/Linux)
#   The Magpi books / special issues (Mac/Linux/Win)
# Hello World
#   Hello World regular issues  (Mac/Linux)
#   Hello World books (Mac/Linux/Win)
# HackSpace Magazine
#   Hackspace Magazine regular issues
#   Hackspace Magazine books (Mac/Linux/Win)
# Wireframe
#   Wireframe regular issues (Mac/Linux)
#   Wireframe books (Mac/Linux/Win)


name: Testing MagPi Regular

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
#  books-powershell:
#    runs-on: windows-latest
#    steps:
#      - uses: actions/checkout@v3.4.0
#
#      - name: make folder for books
#        run: mkdir books
#        shell: bash
#
#      - name: Download Books MagPi windows
#        id: MagPiBooksWin
#        shell: pwsh
#        run: ./windows/downloader.ps1 -downloadUrl "https://magpi.raspberrypi.com/books/projects-5/pdf" -outputDir "books"
#
#      - name: "Check file existence Windows special"
#        id: check_files
#        uses: andstor/file-existence-action@v2.0.0
#        with:
#          files: "books/Pi-Projects5.pdf"
#          fail: true
#
#      - name: lslah books
#        run: Get-ChildItem –path D:\a\downloader\downloader\books
#
#      - name: File exists
#        if: steps.check_files.outputs.files_exists == 'false'
#        run: exit 1

  regular-bash-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3.4.0
      - uses: pCYSl5EDgo/cat@master
        id: read_recent
        with:
          path: https://raw.githubusercontent.com/joergi/MagPiDownloader/main/sources-for-download/regular-issues.txt
      - run: |
          echo "ISSUE_NUMBER=${{ steps.read_recent.outputs.text }}" >> $GITHUB_ENV
          echo "ISSUE_MINUS_ONE=$((${{ steps.read_recent.outputs.text }}-1))" >> $GITHUB_ENV
        shell: bash

      - name: print out vars
        id: printOutVars
        shell: pwsh
        run: env


      - name: make folder for regular
        run: mkdir regular

      - name: Download regular linux
        id: regularIssuesFirstLast
        run: bash ./linux_mac//downloader.sh https://magpi.raspberrypi.com/issues/%02d/pdf regular 120 -f 50 -l 52

      - name: Download regular linux
        id: bookIssuesLast
        run: bash ./linux_mac//downloader.sh https://magpi.raspberrypi.com/issues/%02d/pdf regular 120 -l 2

      - name: lslah
        run: ls -lah regular/

      - name: "Check file existence Linux special issues"
        id: check_files
        uses: andstor/file-existence-action@v2.0.0
        with:
          files: "regular/MagPi01.pdf, regular/MagPi02.pdf, regular/MagPi50.pdf, regular/MagPi51.pdf, regular/MagPi52.pdf"
          fail: true

      - name: File exists
        if: steps.check_files.outputs.files_exists == 'false'
        run: exit 1

  regular-bash-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3.4.0

      - name: make folder for regular
        run: mkdir regular

      - name: Download regular mac
        id: regularIssuesFirstLast
        run: bash ./linux_mac//downloader.sh https://magpi.raspberrypi.com/issues/%02d/pdf regular 120 -f 50 -l 52

      - name: Download regular mac
        id: bookIssuesLast
        run: bash ./linux_mac//downloader.sh https://magpi.raspberrypi.com/issues/%02d/pdf regular 120 -l 2

      - name: lslah
        run: ls -lah regular/

      - name: "Check file existence mac regular"
        id: check_files
        uses: andstor/file-existence-action@v2.0.0
        with:
          files: "regular/MagPi01.pdf, regular/MagPi02.pdf, regular/MagPi50.pdf, regular/MagPi51.pdf, regular/MagPi52.pdf"
          fail: true

      - name: File exists
        if: steps.check_files.outputs.files_exists == 'false'
        run: exit 1
